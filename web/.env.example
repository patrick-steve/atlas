# Atlas — web app environment.
#
# All variables are optional. The site builds and deploys with NONE of them set;
# they only enable the LIVE demo mode by pointing the browser at a tunneled
# local gateway.

# Public URL of the local results gateway (Phase 3).
# Examples:
#   http://localhost:8080
#   https://atlas-tunnel.example.cloudflareaccess.com
NEXT_PUBLIC_GATEWAY_URL=
